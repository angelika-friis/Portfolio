import { translations } from '../i18n/translations';

export const projectsByLanguage = {
  en: translations.en.projects,
  sv: translations.sv.projects,
};

export const projects = projectsByLanguage.en;
